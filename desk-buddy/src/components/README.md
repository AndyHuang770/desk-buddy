# Shared UI components

Kyle owns this directory. Extract reusable cards, navigation, charts, and form controls here as the UI grows. Import shared API types from `src/types/contracts.ts`; do not duplicate the response contract.
